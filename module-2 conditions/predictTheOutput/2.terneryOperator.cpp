// module 2 video no. 13
#include<iostream>
using namespace std;
int main(){
    int x;
    x = 5 > 8 ? 10 : 1 != 2 < 5 ? 20 : 30;
    // here we have to use hiarchy,
   
    // remember the hiarchy () [] ++ -- , * / %, + - , > >=, < <= , == != , &&, ||, ?:, = += -= *= 
   
    cout<<"value of x : "<<x;
}